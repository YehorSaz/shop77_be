export enum ListSocketEnum {
  LIST_SHARED = 'list-Shared',
  LIST_DELETED = 'list-deleted',
  LIST_UPDATED = 'list-updated',
  ITEM_ADDED = 'item-added',
  ITEM_DELETED = 'item-deleted',
  ITEM_UPDATED = 'item-updated',
}
